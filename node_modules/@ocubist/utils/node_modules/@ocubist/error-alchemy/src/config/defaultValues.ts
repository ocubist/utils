import { severitySelector } from "../severity/severitySelector";
import { errorCodeSelector } from "../error-code/errorCodeSelector";

export const DEFAULT_MODULE = "unknown_module";
export const DEFAULT_CONTEXT = "unknown_context";
export const DEFAULT_ERROR_CODE = errorCodeSelector.UNKNOWN_ERROR;
export const DEFAULT_SEVERITY = severitySelector.unexpected;
